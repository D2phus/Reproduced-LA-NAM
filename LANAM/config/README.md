`config.yaml`: default configuration. 

`sweep.yaml`: training hyperparameter tuning. 

`pcc_acc_vs_concurvity.yaml`: Pearson correlation coeffcient-based regularization $\lambda$ tuning. 

`hsic_acc_vs_concurvity.yaml`: HSIC-based regularization $\lambda$ tuning. 
